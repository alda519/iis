%h2 Edit Licence Restriction

=render 'form'

%p= link_to "Destroy", licence_restrict_path, :confirm => 'Are you sure?', :method => :delete

%p= link_to "Back to licence restrictions", licence_restricts_path

