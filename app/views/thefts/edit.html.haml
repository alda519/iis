%h2 Edit Vehicle Theft

=render 'form'

%p
    -if @current_user.admin?
        = link_to "Destroy", theft_path, :confirm => 'Are you sure?', :method => :delete, :class => 'btn btn-danger'
    = link_to "Back to thefts", thefts_path, :class => 'btn'
