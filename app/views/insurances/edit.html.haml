%h2 Edit Insurance

=render 'form'

%p
    = link_to "Destroy", insurance_path, :confirm => 'Are you sure?', :method => :delete, :class => 'btn btn-danger'
    = link_to "Back to insurances", insurances_path, :class => 'btn'
