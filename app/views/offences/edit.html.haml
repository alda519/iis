%h2 Edit Offence

=render 'form'

%p
    -if @current_user.admin?
        = link_to "Destroy", offence_path, :confirm => 'Are you sure?', :method => :delete, :class => 'btn btn-danger'
	= link_to "Back to offences", offences_path, :class => 'btn'
