%h2 Edit Offence

=render 'form'

%p
    -if @current_user.judge?
        = link_to "Revoke this offence", offence_path, :confirm => 'Are you sure?', :method => :delete, :class => 'btn btn-danger'
    = link_to "Back to offences", offences_path, :class => 'btn'
